name: Caller Workflow

on:
  workflow_dispatch:

jobs:
  dev:
    uses: ./.github/workflows/Q6-reusable-workflow.yml
    with:
      target_env: dev

  prod:
    needs: dev
    uses: ./.github/workflows/Q6-reusable-workflow.yml
    with:
      target_env: prod
